// Ruins use the tower ruins sprite when that asset exists in the project.
ruins_sprite_index = s_tower_ruins;
y_sort_enabled = true;

if (ruins_sprite_index != -1)
{
	sprite_index = ruins_sprite_index;
}
